I've set up Google OAuth and added two Replit Secrets: GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET. The redirect URIs registered with Google are:
- https://ethioproperty.com/auth/google/callback
- [my current Replit preview URL]/auth/google/callback

Note: this OAuth app is currently in "Testing" mode, so only email addresses I've added as test users can successfully sign in right now — that's expected, not a bug, until I publish the app later.

There's already a "Welcome Back!" sign-in modal on the site ("Sign In to create listing, save favorites, save search and many more" + a "Continue with Google" button + email/password fields), likely triggered by a person/account icon in the navbar, or by clicking Favorites/save-search while logged out. Please first locate exactly what triggers this modal, then implement Google Sign-In:

1. Check the existing auth system (users table, password_reset_tokens, any session/Passport.js setup already in extensions/features.js or the compiled bundle) to see what pattern is already used for login/sessions
2. Add Google OAuth as an additional login method using GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET — implement via passport-google-oauth20 (or an equivalent lightweight OAuth flow if Passport isn't already present)
3. Add routes /auth/google (start OAuth) and /auth/google/callback (handle response) in extensions/features.js
4. On successful Google login: if a user with that email already exists in the users table, log them in; if not, create a new user record using their Google name/email, matching whatever fields the existing users table already has
5. Wire the existing "Continue with Google" button in that modal to actually call /auth/google instead of being static/non-functional

Don't build the Google Sheets sync yet — that's a separate follow-up once I've set up a service account. Just get sign-in working and confirm end-to-end: locate the trigger for the modal, click Continue with Google, complete login with one of my test user emails, confirm a session/logged-in state actually results, and confirm a new row appears in the users table if it was a first-time signup.

Report back what triggers the modal, and confirm sign-in is working, and flag if anything in the redirect URI or client setup needs adjusting.